# robots.txt for Marino Tactical Training, Inc.
# TEMPORARY PREVIEW DOMAIN: indexing disabled until the production domain is live.
# When the real domain is connected, restore the Allow rules + AI crawler block
# and remove the noindex meta tags from each page's <head>.

User-agent: *
Disallow: /
